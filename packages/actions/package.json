{
  "name": "@getnema/actions",
  "version": "0.1.4",
  "private": true,
  "description": "Nema composite GitHub Actions: gate, approval→flip, freshness",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "check", "approve", "freshness"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@getnema/core": "workspace:*",
    "@getnema/gates": "workspace:*",
    "@getnema/producer": "workspace:*",
    "@getnema/provenance": "workspace:*",
    "@getnema/schema": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
