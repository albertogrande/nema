{
  "name": "@getnema/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Nema — open-source, AI-native documentation platform (monorepo root)",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "turbo run lint typecheck test build",
    "smoke": "node scripts/smoke-publish.mjs",
    "demo:concurrent": "node scripts/demo-concurrent.mjs",
    "version:packages": "changeset version && biome format --write .",
    "release": "pnpm -r publish --no-git-checks --access public"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@changesets/cli": "^2.27.10",
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "turbo": "^2.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
