{
  "name": "@nema/example-minimal",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal content-only Nema corpus — an end-to-end gate fixture (no renderer)",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@nema/gates": "workspace:*",
    "vitest": "^2.1.8"
  }
}
